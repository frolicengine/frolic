{
  "packages": ["packages/*"],
  "npmClient": "yarn",
  "useWorkspaces": true,
  "version": "0.0.0",
  "command": {
    "version": {
      "exact": true
    },
    "publish": {
      "npmClient": "yarn",
      "allowBranch": ["master", "canary"],
      "registry": "https://registry.npmjs.org/"
    }
  }
}
