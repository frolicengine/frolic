// import { PI } from "./math.ts";
// import Vector from '../math/Vector.ts';

// export const toDegrees(radians: number) {
//   return radians * (180 / PI);
// }

// export const toRadians(degrees: number) {
//   return degrees * (PI / 180);
// }

// export const rotate(vector: Vector, angle: number) {
//   const x = vector.x * math.cos(angle) - vector.y * math.sin(angle);
//   const y = vector.x * math.sin(angle) + vector.y * math.cos(angle);

//   return { x, y };
// }

// getAngle(x, y) {
//   return math.atan2(y, x);
// }

// getTo({ y, x }, { y, x }) {
//   return math.atan2(y - y, x - x);
// }

// toAxis(angle, radius) {
//   return [math.cos(angle) * radius, math.sin(angle) * radius];
// }

// getBetween(vector:Vector, vector2:Vector) {
//   const dot = vector.dot(vector2);

//   return math.cos(dot / (vector.getMag() * vector2.getMag()));
// }
